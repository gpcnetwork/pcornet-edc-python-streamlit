[project]
name = "pcornet-dq-dashboard"
version = "1.0.0"
description = "PCORnet CDM v7.0 Data Quality Dashboard — Streamlit in Snowflake"
requires-python = ">=3.11"
dependencies = [
    "streamlit[snowflake]",
    "duckdb>=1.0.0",
    "jinja2>=3.0",
    "pandas>=2.0.0",
    "snowflake-snowpark-python",
    "keyring",
    "python-dotenv",
    "transformers",
    "torch",
    "torchvision",
]
